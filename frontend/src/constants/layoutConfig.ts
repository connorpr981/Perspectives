import { LayoutConfig } from '../types/pathBased';

export const defaultLayoutConfig: LayoutConfig = {
  columns: [
    { title: 'Sections' },
    { title: 'Questions' },
    { title: 'Assertions' },
  ]
};